{
  "name": "gds-di-onboarding-dynamo-api",
  "description": "DI self-service DynamoDB API",
  "scripts": {
    "build": "sam build --beta-features",
    "lint": "eslint '*.ts' --quiet --fix",
    "compile": "tsc",
    "test": "jest --silent"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.160.0",
    "@aws-sdk/client-lambda": "^3.160.0",
    "@aws-sdk/client-sfn": "^3.160.0",
    "@aws-sdk/client-sns": "^3.162.0",
    "@aws-sdk/client-sts": "^3.160.0",
    "@aws-sdk/util-dynamodb": "^3.160.0",
    "axios": "^0.27.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.102",
    "@types/node": "^18.7.14",
    "@types/jest": "^29.0.0",
    "esbuild": "^0.15.6"
  }
}
