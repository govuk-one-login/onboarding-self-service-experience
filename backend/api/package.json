{
  "name": "gds-di-self-service-api",
  "description": "DI self-service API",
  "scripts": {
    "build": "sam build --beta-features",
    "lint": "eslint '*.ts' --quiet --fix",
    "compile": "tsc",
    "test": "jest --silent"
  },
  "dependencies": {
    "@aws-sdk/client-sfn": "^3.266.0",
    "@aws-sdk/client-sns": "^3.266.0",
    "esbuild": "^0.15.6"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.102"
  }
}
