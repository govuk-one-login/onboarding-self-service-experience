{
  "name": "gds-di-self-service-api",
  "description": "DI self-service API",
  "scripts": {
    "build": "../../infrastructure/api/deploy-sse-api.sh --no-deploy",
    "deploy": "../../infrastructure/api/deploy-sse-api.sh",
    "lint": "eslint '*.ts' --quiet --fix",
    "compile": "tsc",
    "test": "jest --silent"
  },
  "dependencies": {
    "@aws-sdk/client-sfn": "^3.306.0",
    "@aws-sdk/client-sns": "^3.306.0"
  }
}
