name: Check PR

on: pull_request
permissions:
  id-token: write
  contents: read

jobs:
  code-quality:
    name: Code quality
    uses: ./.github/workflows/check-code-quality.yml

  run-unit-tests:
    name: Run tests
    permissions: { }
    uses: ./.github/workflows/run-unit-tests.yml

