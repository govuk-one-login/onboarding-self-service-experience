{% extends "layout.njk" %}
{% from "macros/back-link.njk" import backLink %}
{% from "macros/error-summary.njk" import errorSummary %}
{% from "govuk/components/input/macro.njk" import govukInput %}

{% block pageTitle %}Confirm phone number - GOV.UK Sign In{% endblock %}

{% block beforeContent %}
  {{ backLink("/security-check-change-number") }}
{% endblock %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">

      {{ errorSummary ({
        errors: errors,
        hrefs: {
          'mobileNumber': '#mobileNumber'
        }
      }) }}

      <h1 class="govuk-heading-l">Confirm the phone number currently linked to your account</h1>

      <form class="form" action="/confirm-phone-number" method="post">
        {{ govukInput({
          label: {
            text: "Phone number"
          },
          name: "mobileNumber",
          id: "mobileNumber",
          classes: "govuk-input--width-20",
          type: "tel",
          autocomplete: "tel",
          value: value.mobileNumber,
          errorMessage: {
            text: errors.get('mobileNumber')
          } if errors else false
        }) }}

        {{ govukButton({
          text: "Continue",
          attributes: {"id": "submit"}
        }) }}
      </form>

    </div>
  </div>
{% endblock %}
